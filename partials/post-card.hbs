<article class="post-card {{post_class}}">
    <a class="post-card-content-link" href="{{url}}">
        <header class="post-card-header">
            <h2 class="post-card-title">$> {{title}}</h2>
            <span class="reading-time">{{reading_time}}</span>
            {{#has tag="sponsored" author="value"}}
                <span class="sponsored-tag">{{t "sponsored"}}</span>
            {{/has}}
        </header>
        <section class="post-card-excerpt">
            <p>{{excerpt words="50"}}</p>
        </section>
    </a>
    <footer class="post-card-meta">
        {{t "Posted"}}
        {{#if primary_tag}}
            {{t "in"}} {{#primary_tag}}<a href="{{url}}" class="post-card-tags">{{name}}</a>{{/primary_tag}},
        {{/if}}
        {{t "on"}}
        <time class="post-date" datetime="{{date format="YYYY-MM-DDTHH:mm:ss.SS\Z"}}">{{date}}</time>
    </footer>
</article>
