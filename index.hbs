{{!< default}}

<div class="mds-page">
    <div class="mds-container">

        <div class="mds-postfeed">
        {{#foreach posts}}

            {{> "card"}} {{!-- partials/card.hbs --}}

        {{/foreach}}
        </div>

    </div>
</div>